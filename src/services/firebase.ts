// TODO: Initialize Firebase Auth + Firestore
// TODO: Add auth state listener and expose helper methods for login, role persistence, and event CRUD.

export const firebasePlaceholder = {
  initialized: false,
}
